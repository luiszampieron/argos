[project]
name = "argos"
version = "0.1.0"
description = "Core DDD para controle de tarefas em equipe"
requires-python = ">=3.11,<3.14"
dependencies = [
  "argon2-cffi==23.1.0",
  "fastapi==0.136.3",
  "PyJWT==2.10.1",
  "pydantic==2.9.2",
  "uvicorn==0.30.6",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
  "alembic==1.16.5",
  "SQLAlchemy==2.0.43",
]
