[project]
name = "argos"
version = "0.1.0"
description = "Core DDD para controle de tarefas em equipe"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi==0.136.3",
  "pydantic==2.9.2",
  "uvicorn==0.30.6",
]

[tool.uv]
package = false
